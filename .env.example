# Copy this file to .env.local and fill in to enable REAL AI generation.
# Without it, the app runs in Mock mode (returns a demo output, no API calls).

OPENAI_API_KEY=
# Optional: point to OpenRouter / Azure / any OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
